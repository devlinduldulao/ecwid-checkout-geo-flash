{
  "name": "checkout-geo-flash-ecwid",
  "version": "0.1.0",
  "description": "Static Ecwid storefront and admin app for Checkout Geo Flash using Ecwid App Storage public config.",
  "main": "src/shared/checkout-geo-flash-shared.js",
  "scripts": {
    "build": "node scripts/build-static.js",
    "capture:marketplace": "node scripts/capture-marketplace-screenshots.js --base-url http://127.0.0.1:5010",
    "dev": "serve . -l 5000",
    "preview": "serve dist -l 5001",
    "test": "node --test tests/**/*.test.js",
    "lint": "node --check src/shared/checkout-geo-flash-shared.js && node --check src/shared/dashboard-preview.js && node --check src/admin/app.js && node --check src/storefront/custom-storefront.js && node --check scripts/build-static.js && node --check scripts/capture-marketplace-screenshots.js"
  },
  "keywords": [
    "ecwid",
    "lightspeed",
    "ecommerce",
    "plugin",
    "social-proof"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright": "^1.59.0",
    "serve": "^14.2.4"
  }
}
